export class frmBusquedaModel {
    constructor(
        tramiteServicio: string,
        dependencia: number,
        kiosco: number,
        tramite: number,
        desde: string,
        hasta: string
    ){}
}